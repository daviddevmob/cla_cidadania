import 'package:flutter/material.dart';

class AdmEditProfileScreen extends StatelessWidget {
  const AdmEditProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
